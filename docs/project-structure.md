# Project structure

This document lists every planned folder and file in the Marshal repo, and what each one is for. It is the map to check before creating, moving, or deleting a file.

`code-standards.md` section 1 gives the short folder overview and the rules. This document is the full file-level list.

---

## How to keep this document true

- **Adding, moving, renaming, or deleting a file** means updating this document in the same pull request, and adding a row to the file changes log in `progress-tracker.md`.
- **Tests** sit next to the file they test and are not listed one by one: `session.go` has `session_test.go`, and `CardItem.tsx` has `CardItem.test.tsx`.
- **Generated files** are marked as generated. They are never edited by hand.
- **Planned, not final.** Files here are the plan. When real work shows a better split, change the plan here first.
- **Naming:** Go files use lower case with underscores. Component files use PascalCase. Other TypeScript files use kebab-case.
- **No Go file may be named `test.go` or end in `_test.go` unless it holds tests.** Connection test code lives in files named `conntest.go`.

---

## 1. Root

```
marshal/
  package.json                  Root pnpm scripts: dev, build, check, smells, gen, and more
  pnpm-workspace.yaml           Workspace packages
  pnpm-lock.yaml                Lock file (generated by pnpm)
  .npmrc                        pnpm settings
  .editorconfig                 Shared editor settings
  .gitignore
  .gitattributes                Line endings and generated file markers
  tsconfig.base.json            Shared TypeScript settings
  biome.json                    TypeScript formatting and lint rules, including smell limits
  biome.warn.json               Soft limits (the warning levels), run by pnpm smells
  knip.json                     Unused files, exports, and dependencies check
  .jscpd.json                   Duplicated code check across languages
  .golangci.yml                 Go lint rules with the blocking smell limits
  .golangci.warn.yml            Go soft limits (the warning levels), run by pnpm smells
  .smells-baseline.json         Accepted smells with reasons (starts empty)
  README.md                     What Marshal is, and how to get started, linking to docs/ (planned)
  LICENSE                       (planned)
  .github/
    workflows/
      ci.yml                    On every pull request and push to main: the light checks first (generated files, format, lint, types, smells, contrast), then tests with coverage floors, build, and budgets on all three platforms, then the browser suite on Linux
      e2e-macos.yml             The browser suite on macOS, after ci passes on main (never on a pull request)
      nightly-agent-smoke.yml   The real agent CLIs through one small card each, nightly. Off until turned on with a repository variable; needs a self-hosted Mac with the CLIs signed in
      nightly.yml               Real agent smoke tests against pinned CLI versions (planned)
      release.yml               Build, sign, and publish installers and daemon packages (planned)
    pull_request_template.md    What changed, why, how it was tested, budget impact, smells
  docs/                         Project docs (section 2)
  daemon/                       Go daemon and command line tool (section 3)
  apps/
    web/                        SolidJS app (section 4)
    desktop/                    Tauri shell (section 5, planned)
  packages/
    ui/                         Component library (section 6)
    tokens/                     Design tokens (section 7)
    protocol/                   Generated API types (section 8)
  tools/                        Development tools (section 9)
  scripts/                      Cross-platform scripts used by pnpm (section 10)
  design/                       The original design prototype, read-only reference
  .tools/                       Local Go tool binaries (ignored by Git)
  dist/                         Build output (ignored by Git)
```

---

## 2. Docs

```
docs/
  README.md                     Index of all docs and reading order
  marshal-product-scope.md      Every feature, how it works, and why
  project-overview.md           What we are building and why. Read first.
  architecture.md               How the system is built
  project-structure.md          This document
  build-plan.md                 What we build, in what order
  code-standards.md             How we write code, including code smells
  development.md                How to run, test, and build
  library-docs.md               Approved libraries and how to use them
  ui-tokens.md                  Design values
  ui-rules.md                   How the UI looks, reads, and behaves
  ui-registry.md                Every UI component
  design-port.md                How the Claude Design became the app, and approved deviations
  backend-checklist.md          The backend build and cutover checklist
  backend-inventory.md          Every prototype action and field, mapped to the backend
  progress-tracker.md           Live status, decisions, questions, and file changes
```

---

## 3. Daemon

**How to read this section.** Files that exist today are listed as they are, verified against the
checkout on 2026-09-25. Files that belong to later phases are collected in the "Planned for later
phases" block at the end of this section; when one is built, move it out of that block in the same
change.

```
daemon/
  go.mod                        Go module and pinned toolchain
  go.sum
  package.json                  pnpm scripts that wrap Go commands (no Node code)
  .air.toml                     Reload settings for pnpm dev
  sqlc.yaml                     SQL to Go generation settings (run by pnpm gen, output in internal/store/db)
  tygo.yaml                     Go to TypeScript type generation settings

  cmd/
    marshald/
      main.go                   Daemon entry point: flags, config, start modules, and the dev fixture (--fixture prototype) before sessions are restored
      output.go                 Writes lines to the terminal
      terminals.go              Registers the PTY adapter so a card can switch to the terminal view, and its interactive command per agent
    marshal/
      main.go                   Command line tool entry point
      output.go                 Writes lines to the terminal
      cmd_status.go             marshal status: is the daemon running, and on which port
      cmd_token.go              marshal token: where the token file is, or the token itself with --show
      cmd_dev.go                Dev-only commands (marshal dev reset, which only ever deletes the dev data folder)
      cmd_service.go            marshal service install, uninstall, status: installs marshald (not marshal itself) as the per-user login service

  internal/
    buildinfo/
      buildinfo.go              The version number that the daemon, the tool, and the app share

    protocol/                   Wire types and the small rules that belong to them, generated into packages/protocol (architecture.md section 11.5)
      doc.go                    Package comment
      health.go                 The health answer
      time.go                   Timestamp: UTC, RFC 3339, milliseconds
      ids.go                    Project ids, slugs, and card keys
      opaque_id.go              Opaque ids (ULIDs) and their validity check
      errors.go                 Error codes, the one error shape, and its helpers
      page.go                   Page of a list
      enums.go                  The fixed lists: card states, permission and thinking modes, agents, sessions, feed, notices, activity, CI, card views
      event_types.go            Event type names
      session_events.go         Payloads of session.output, session.tool_call, and session.state_changed
      card_actions.go           The body of a message to a card's agent, and its length limit
      events.go                 Event, event batch, hello, resync, and the WebSocket subprotocol names
      topics.go                 Topics, their constructors, and the parser
      project.go                Project, its source, and the create, update, and remove request bodies
      project_events.go         Payloads of project.created, project.updated, project.removed, card.created, card.updated, card.moved, card.deleted, and label.updated
      card.go                   Card, board snapshot, the create-card request, the update-card request, the move request, and the date change
      label.go                  A project's labels: the label, its snapshot, and the create and update requests
      home.go                   The Home dashboard answer: the needs-you and awake lists and the tile counts
      agents.go                 The agent catalog: agents, models, capabilities, and their statuses
      auth.go                   WhoAmI and the device kinds
      conventions_test.go       Checks the package's own source against the rules (no bare time.Time, server time, enum lists)
      *_test.go                 Table tests and golden files for each rule

    testutil/
      fixtures.go               Fixture repositories, copied to temp folders and committed for tests
      golden.go                 Golden files shared with the app's tests
      stubagent.go              Builds tools/stub-agent once per test process and returns the path of the program
      terminalhelper.go         Builds the small program the terminal tests run in a pseudo-terminal as the "CLI"

    fixture/
      fixture.go                LoadPrototype: makes the prototype's three projects (api, web, mobile) from the repos in testdata, and writes their cards, safe to run on every start
      cards.go                  The prototype's 29 cards as a data table, and the loader that writes them and their labels
      repo.go                   Makes each project's Git repository in <data>/fixtures (a copy, one commit with a fixed identity and date), and finds testdata/repos at run time
      *_test.go                 Tests against a real store and real Git: repeat loads, missing pieces, broken folders, Git missing, and the fixed identity

    platform/
      paths.go                  Data folder per OS, dev and normal
      devtoken.go               The dev token file
      reset.go                  Guard and delete for the dev data folder
      lock.go                   Single instance lock: exported AcquireLock/Release, ErrAlreadyRunning
      lock_unix.go              flock(2) through golang.org/x/sys/unix (macOS, Linux)
      lock_windows.go           LockFileEx through golang.org/x/sys/windows, on a byte range past the pid text so it stays readable
      service.go                ServiceInstaller interface, ServiceSpec, ServiceStatus, and the shared admin-command runner
      service_launchd.go        macOS: launch agent plist generation, launchctl bootstrap/bootout/print, and their parsing
      service_systemd.go        Linux: systemd user unit generation, systemctl and loginctl calls, and their parsing
      service_schtasks.go       Windows: scheduled task argument generation for schtasks, and its output parsing
      service_darwin.go         NewServiceInstaller for macOS (wires service_launchd.go)
      service_linux.go          NewServiceInstaller for Linux (wires service_systemd.go)
      service_windows.go        NewServiceInstaller for Windows (wires service_schtasks.go)
      tokens.go                 Token creation, the token file's path per mode, and its permissions
      *_test.go                 Tests for the lock, the paths, the dev reset guard, the token files, and the installer argument and output handling (no test ever installs anything)

    proc/                       The one helper that starts child processes (never through a shell)
      proc.go                   Start, Process (Wait, Done, Pid, StderrTail), and Stop: polite first, then the whole process tree
      pipes.go                  Output and input pipes that outlive the child's exit
      env.go                    The environment allow-list and the extra entries a caller adds
      tail.go                   The bounded tail of standard error
      proc_unix.go              Process groups and signals (SIGTERM, then SIGKILL to the group)
      proc_windows.go           New process group, hidden window, and taskkill /T /F
      tree.go                   Environ, Polite, KillTree, and Sweep, for code that starts a child by another route (the PTY adapter)
      *_test.go                 Tests that start the test binary itself as the child (helper process pattern)

    config/
      config.go                 Settings from flags, MARSHAL_* environment settings, and defaults (config.toml comes later)
      defaults.go               Default settings and limits

    api/
      server.go                 HTTP server, binding to loopback only (the tailnet binding is Phase 9)
      router.go                 The one router: public and protected route registration
      routes.go                 The table of domain routes and their registration (a route exists only when the service it needs does), the shared id checks, and the longer time to answer for start and resume
      routes_projects.go        Projects: list, create, read, rename, edit, remove, the board, and creating a card
      routes_cards.go           Cards: read, start, send a message, stop, resume
      routes_agents.go          Agents: list and refresh
      routes_me.go              The person: profile, avatar (the upload and the image), users list, progress, preferences, and the dev-only reset of first launch
      routes_saved_views.go     A project's saved views: list, save, change, delete
      routes_hold.go            Session hold: pause, unpause, sleep, wake, pin, unpin (the rules in architecture.md 5.1)
      routes_view.go            The chat/terminal switch (POST /v1/cards/{id}/view)
      routes_history.go         A card's paged chat and activity history, and a chat's
      routes_diff.go            A card's changed files, and one file's hunks
      routes_labels.go          A project's labels: list, create, rename, recolor, delete
      routes_chats.go           A project's chats: list, create, rename, archive, restore, delete, and send a message
      routes_dashboard.go       Home: the dashboard and the activity stream
      routes_search.go          Search over projects, cards, and chats for the palette
      route_errors.go           Turns the errors the services leave as plain Go errors (an agent that needs a sign-in, a branch left over, no agent running) into plain sentences
      auth.go                   Bearer token checks for every protected route
      whoami.go                 GET /v1/auth/whoami
      accounts.go               The owner device, and the dev token on start
      accesslog.go              One line per request: method, path, status, duration (never a header or a token)
      bodylimit.go              Caps every request body before a handler can read it
      decode.go                 Strict JSON decoding: one value, no unknown fields, the size limit, plain sentences
      errors.go                 Turns any error into the one error shape and its HTTP status
      respond.go                Writes JSON answers
      paging.go                 Reads limit and cursor, and encodes and decodes cursors
      limits.go                 Sizes and times the server works within, including the 25 ms event flush interval
      recover.go                Turns a panic in a handler into a 500 in the one error shape
      requestid.go              A per-request id for the access log
      statuswriter.go           Remembers the status code a handler wrote
      stream.go                 GET /v1/events: the upgrade, the hello, and the reader and writer goroutines
      stream_client.go          Decodes the client's frames: hello, and the three terminal messages
      stream_terminal.go        Serves a card's terminal on the stream: input, resize, and the snapshot
      stream_hub.go             One stream's connection, its private queue, and how a stuck client is cut
      stream_pump.go            Batches events for one stream and writes them as frames
      stream_origin.go          The origin check for the upgrade
      hijack.go                 Takes the connection over for the upgrade, and gives it back
      *_test.go                 Tests against a real server on a free port with a real store, bus, projects module, and session manager over the stub agent, and the event stream through a real WebSocket client

    events/
      bus.go                    In-process publish and subscribe: sequence numbers, the replay ring, Since and SubscribeSince
      event.go                  Event, Replay (ok, too-old, other-epoch, ahead), and why a subscription closed
      filter.go                 Topic filters (exact topics or all), and the helper that filters a replay
      subscription.go           One subscriber: bounded queues, drop rules for slow ones, one goroutine
      queue.go                  The growing first-in first-out list behind the queues and the ring

    accounts/
      service.go                Profile, preferences, and progress: the module the person's routes call
      profile.go                Read and edit the profile; the initials and the avatar URL on the wire
      avatar.go                 The avatar upload: the kind read from the bytes, the stored file, and its version
      preferences.go            Screen preferences, merged field by field, and the me.updated event
      progress.go               Onboarding and tutorial progress, and the dev-only reset of first launch

    cardhistory/
      service.go                The read routes over the history index: a card's chat and activity, paged

    chats/
      service.go                A project's chats: create, list, rename, archive, restore, delete, and send a message

    dashboard/
      service.go                The home dashboard: needs you, tiles, and the coming-up list
      mapping.go                Rows to the dashboard's wire shapes
      subscriber.go             Publishes activity.created on the home topic as the history index grows

    diff/
      service.go                A card's changed files with counts, and one file's hunks, over internal/gitx

    history/
      history.go                Append and page the session history index, and the cursor
      wire.go                   History rows to the wire message and activity types
      kinds.go                  The kind and state vocabularies, and the mapping from an agent event to a record
      agents.go                 The narrow interface the session manager records through, so a failure never stops the pump
      doc.go                    Package comment

    keyedlock/
      keyedlock.go              A map of locks by key: two calls for one key run one after the other, different keys do not wait

    sample/
      sample.go                 The sample repository that ships in the daemon: write it once, and refuse a second

    search/
      search.go                 Search over projects, cards, and chats, through the services and with no index
      match.go                  The matching and ranking rules, and the per-kind cuts

    projects/
      service.go                Projects module interface and wiring
      projects.go               Read, rename, edit, and the Project record
      create.go                 Create from a folder or a clone, with rollback when it fails
      remove.go                 Remove: Marshal's branches and memory, per the two options, and never the repo folder
      boards.go                 One board per project, columns
      cards.go                  Card data and actions
      cardstate.go              Card state machine and allowed moves
      detect.go                 Language, dev command, and monorepo detection
      monorepo.go               Monorepo package detection
      card_labels.go            Loading the labels of one card, or of every card on a board
      fork.go                   Forking a card, and the branch a fork's branch starts from
      labels.go                 The labels of a project: create, list, rename, recolor, delete
      saved_views.go            A project's saved views: list, save under a name (a used name replaces), change, delete
      move.go                   The manual-move rules of architecture.md 6.1, and the move itself
      remove_card.go            Deleting one card: its session, worktree, branch, logs, and row
      hold.go                   Pause, unpause, pin, and unpin: the card's own fields, published as card.updated
      sample.go                 The sample project source: POST /v1/projects with source "sample"
      update.go                 Editing the fields of a card that a request sets
      mapping.go                Store rows to wire types, including every card's labels
      paths.go                  Worktree and branch paths for a project
      locks.go                  Per-project locks so two creates cannot race
      defaults.go               Card defaults
      errors.go                 Plain errors callers match
      *_test.go                 Tests against a real store and real Git, including a remove that proves the repo folder is untouched

    session/
      doc.go                    Package comment
      config.go                 Config, ResumeMode, and their defaults
      manager.go                Manager and its bookkeeping: NewManager, RecentOutput, the live-session map, reserve/release
      start.go                  Start: worktree, agent start, the session row, and undoing a failed start
      send.go                   Send: claims a turn or queues behind one that is running, and reconciles with the agent's own busy answer
      stop.go                   Stop: ends the agent process and marks the session row stopped
      close.go                  Close: the manager's own shutdown, which never marks an awake session stopped
      restore.go                RestoreAll and Resume, and a failed resume moving the card to needs you
      pump.go                   The per-session event loop: log, ring, publish output and tool calls, react to TurnEnded and an ended session
      live.go                   liveSession: one running session's in-memory bookkeeping (busy, queue, stop-requested)
      log.go                    Session logs to disk: JSON-lines segments, rotation, and the flush timer
      ring.go                   The bounded in-memory ring of recent log entries
      events.go                 Agent event to a typed log line, and to the session.output/session.tool_call wire payloads
      states.go                 The stored session states, and the card's own flags read from session rows
      hold.go                   Session hold: pause, sleep, wake, and pin on a live session (architecture.md 5.1)
      chat.go                   A project chat's own session: start, resume, send, stop, and its settings
      terminal.go               A card's terminal: input, resize, and the screen a viewer paints (live-only output)
      view.go                   Switching a card between the chat view and the terminal view, and its refusals
      recorder.go               Writes the history index as events happen, with failures logged and swallowed
      owner.go                  cardOwner and chatOwner: one session's owner, a card or a chat
      seed.go                   A stop-safe seed for ids, derived from the data folder
      project_hooks.go          StopProjectSessions: a removed project stops its own sessions and no other
      *_test.go                 Tests with a fake agents.Agent and, once, the real stub agent through agents/acp

    agents/
      agent.go                  Agent interface, StartSpec, SessionHandle, and Capabilities
      events.go                 The closed set of agent events and their value words, including TerminalOutput from the PTY adapter
      errors.go                 ErrBusy, ErrCannotResume, and the other errors callers act on, and AuthRequiredError
      registry.go               Registry: agent kind to factory
      eventsink.go              The shared event channel rules: wait for room, give up on release, deliver a final event, close once
      truncate.go               Cuts event text to its bound without splitting a character
      acp/
        config.go               Adapter settings: program, arguments, environment, timeouts
        adapter.go              ACP agent adapter: the agents.Agent methods and the running sessions
        session.go              One running process and its connection, the event channel, and the end of the session
        handshake.go            Start and resume: hello, sign-in on demand, the session, and its settings
        settings.go             Model, thinking, and permission mode through the agent's modes and options
        turn.go                 Send, the turn goroutine, interrupt, and stop
        permission.go           Permission requests that wait for Respond
        client.go               What the agent calls: updates, permission requests, and the methods that are not offered
        convert.go              Protocol updates to agent events, with the size bound
        logger.go               A quiet logger for the protocol library that drops raw message text
        *_test.go               Tests against the stub agent and a fake agent (the test binary started again in a protocol mode)
      catalog/
        catalog.go              The agent catalog: detection, the five-minute cache, single-flight, and refresh
        specs.go                What each known agent is: kind, name, models, capabilities, install hint
        probe.go                Asks one program for its version, with a timeout
        locate.go               Finds a program on PATH and in the well-known folders
        semver.go               Parses and compares versions
        tested.go               The versions we have tested
        models.go               The models each agent offers
        stub.go                 The dev-mode catalog: every agent reported as supported
        *_test.go               Tests for the cache, refresh, timeouts, version parsing, and the programs found on this machine
      claude/
        config.go               Adapter settings; the permission mode and effort mappings
        args.go                 Argument lists for a new session and for a resumed one
        adapter.go              Claude Code agent adapter: the agents.Agent methods and the running sessions
        session.go              Process lifecycle: launch, wait for ready, watch for exit, and the event channel
        dispatch.go             Reads one stream-json output line and turns it into an event
        turn.go                 Send, Interrupt (control_request, then SIGINT, then a stop-and-resume fallback), and Stop
        interrupt_unix.go       Sends SIGINT to end a turn (Unix)
        interrupt_windows.go    SIGINT has no portable equivalent here; always errors, so Interrupt skips to the fallback
        convert.go              Tool call kind, path, and command, and the turn-end reason
        wire.go                 The stream-json line shapes, in and out
        uuid.go                 Session ids: Claude Code's --session-id needs a UUID, not one of Marshal's own ULIDs
        *_test.go               Tests against a fake claude program (the test binary started again in stream-json mode)
      gemini/
        config.go               Adapter settings: program, arguments, environment, and the resume arguments
        adapter.go              Gemini CLI adapter over ACP, with its AgentFactory
        model.go                Gemini's model list
        *_test.go               Tests against a fake gemini program
      codex/
        codex.go                Codex: detected and reported as missing or found, not startable until it has an adapter
        *_test.go               Tests that it is reported and that it cannot start
      pty/
        config.go               Adapter settings: program, arguments, resume and instruction arguments, size, tee, and the size and timing constants
        adapter.go              PTY agent adapter: the agents.Agent methods, WriteRaw, Resize, Snapshot, and the running sessions
        session.go              One program in a terminal: input, resize, stop (polite, then the whole tree), and the end of the session
        output.go               Reads the terminal, feeds the ring and the tee, and batches output into TerminalOutput events
        ring.go                 The fixed-size ring of the last 256 KiB of output
        pty_unix.go             Line end and the child end of the terminal on Unix
        pty_windows.go          Line end and the polite stop on Windows (ConPTY)
        testdata/helper/        Small program the tests run in the terminal (standard library only, no shell)
        *_test.go               Tests against the helper program, including a 50 MB burst that checks memory stays flat

    gitx/
      git.go                    Runs Git with argument lists, and stops it when the context is cancelled
      version.go                Git version check (2.38 or newer)
      errors.go                 Plain errors callers match: not a repo, dirty, outside root, bad address
      paths.go                  Path checks: full path, no "..", inside a folder, empty or missing
      inspect.go                Repo facts: top folder, branches, remotes without credentials, clean
      worktree.go               Create and remove worktrees, and undo a failed create
      worktreelist.go           List and prune worktrees
      sparse.go                 Sparse checkout limited to folders
      branches.go               Create, check, list, merge-check, and delete branches
      branchname.go             Card branch names, marshal/<project>-<number>-<title>
      cloneurl.go               Checks a clone address and strips credentials from it
      clone.go                  Clone with only the allowed protocols, and no token in errors
      *_test.go                 Tests against real Git in temp folders and the fixture repos in testdata

    store/
      store.go                  Open, Close, Ping, Read, and Write: SQLite in WAL mode, one writer and a pool of readers
      migrate.go                Runs the embedded migrations on start (goose), and the forward-only check
      accounts.go               HashToken (device tokens are stored as SHA-256 hex) and EnsureOwner
      time.go                   Unix milliseconds to the wire Timestamp
      migrations/
        0001_init.sql           Settings, users, devices, and user_progress
        0002_projects.sql       Projects and cards
        0003_sessions.sql       Sessions
        0004_cards.sql          The card fields the screens show, and the labels and card_labels tables
        0005_forks.sql          The card a fork came from, so its branch starts from that card's commit
                                Each later module adds its own numbered file for its own tables.
      queries/                  SQL by area, one file each
        settings.sql            Settings: get and set
        accounts.sql            The owner, and devices: create, find by token hash, list, touch, revoke
        projects.sql            Projects and cards: create, read, list, rename, update, remove, numbering
        sessions.sql            The session row: create, read, state, session id, resume
                                Later modules add their own file here.
      db/                       Generated by sqlc (`pnpm gen`). Do not edit: `accounts.sql.go`, `projects.sql.go`, `sessions.sql.go`, `settings.sql.go`, `models.go`, `db.go`

  testdata/
    repos/
      small-repo/               Small fixture repo (also the source of the prototype fixture's api-gateway and web-dashboard)
      monorepo/                 Fixture monorepo with packages (also the source of the prototype fixture's mobile-app)
    hooks/
      github/                   Recorded GitHub webhook bodies
      trello/                   Recorded Trello webhook bodies
      telegram/                 Recorded Telegram updates
    golden/                     Expected outputs for tests, written by the Go tests and read by the TypeScript tests
```

### Planned for later phases (not built yet)

These files are still to come. They are kept here so the shape of the daemon stays visible; each one
moves out of this block when it is built.

```
  cmd/marshal/
      cmd_cards.go              marshal cards (list, new, start, send)
      cmd_keys.go               marshal keys set, list, remove

  internal/api/
      pairing.go                Device pairing codes
      handlers_boards.go        Saved views and filters
      handlers_cards.go         Cards: move, fork, checkpoints, diff
      handlers_checklists.go    Checklists and items
      handlers_comments.go      Comments and attachment uploads
      handlers_members.go       Card members
      handlers_chats.go         Chats: list, create, rename, archive, restore, delete, messages
      handlers_sessions.go      Sleep, wake, pin, view switch
      handlers_approvals.go     Approve and deny
      handlers_home.go          Dashboard and activity
      handlers_me.go            Profile, devices, onboarding and tutorial progress
      handlers_integrations.go  Integrations and connection tests
      handlers_providers.go     Provider keys and tests
      handlers_mcp.go           MCP servers and tests
      handlers_quality.go       Smell findings and profiles
      handlers_search.go        Session and notes search
      handlers_settings.go      Settings, roles, templates, schedules, limits
      handlers_hooks.go         Webhook routes, signature checks first

  internal/projects/
      views.go                  Filters, swimlanes, saved views
      templates.go              Card templates
      dependencies.go           Card links and auto start
      checks.go                 Acceptance checks
      checklists.go             Checklists, items, required and people-only rules
      evidence.go               Evidence for agent ticks, and auto untick
      members.go                Card members

  internal/session/
      sleep.go                  Idle detection, sleep and wake
      reminders.go              Grouped sleep warnings
      awake_limits.go           Per-project and global awake limits

  internal/gitx/
      checkpoints.go            Checkpoint refs and restore
      diff.go                   Structured diffs
      mergetree.go              Dry-run merges
      cleanup.go                Worktree, branch, and gc cleanup

  internal/agents/
      builtin/
        loop.go                 Built-in agent loop
        tools.go                Read, edit, run, and MCP tools
        adapter.go              Built-in agent adapter

  internal/comments/            Comments, attachments, mentions, and links
  internal/harness/             Control loop around every agent
  internal/providers/           Model providers, rate limits, usage, pricing
  internal/integrator/          The merge queue
  internal/ci/                  CI monitoring and the fix loop
  internal/quality/             Smell checks on card diffs
  internal/scheduler/           Timers, cron, triggers, loops, briefs
  internal/mcpserver/           The internal MCP server
  internal/memory/              The vault, lessons, and search
  internal/codemap/             The codebase map
  internal/integrations/        GitHub, Trello, Google Calendar, Gmail, Telegram, Discord
  internal/notify/              Notice routing
  internal/security/            Permissions, secret scanning, the audit log, keychain
  internal/remote/              tsnet and Funnel
  internal/preview/             Per-card dev servers and screenshots
  internal/budgets/             RAM, CPU, and disk self-measurement
  internal/web/embed.go         Embeds the built UI into the daemon
```

---

## 4. Web app

**How to read this section.** Files that exist today are listed plainly, verified against the
checkout on 2026-09-25. A line ending in `(planned)` is not built yet.

```
apps/web/
  package.json
  index.html
  vite.config.ts                Vite and Solid setup, proxy to the dev daemon
  vite/
    dev-token.ts                Dev server route that gives the dev token to this machine only
  tsconfig.json
  playwright.config.ts          End-to-end tests at phone, tablet, and desktop sizes: starts the built dev daemon and Vite

  src/
    main.tsx                    App entry: boots the store, then the app
    App.tsx                     Providers, theme, routes (planned: AppRoot owns the shell today)

    app/
      routes.ts                 Route list (planned)
      AppRoot.tsx               Shell with top bar, sidebar or bottom navigation, and the screen for each connection state
      AppSkeleton.tsx           The app's own layout as placeholders, while the first data loads
      ConnectionScreens.tsx     Can't reach the daemon, sign in, the offline bar, and a failed first load (mounts the parts from @marshal/ui)
      Workspace.tsx             The main area: view header, filter bar, and the open project's view
      TopBar.tsx, Sidebar.tsx, PhoneNav.tsx, PhoneSheet.tsx, SplitPanes.tsx, DetailPanel.tsx
      ProjectList.tsx, ProjectRow.tsx, ProjectMenu.tsx, ProjectRenameInput.tsx
      FilterBar.tsx, FilterMenu.tsx, FilterMenus.tsx, FilterPopover.tsx, SavedViewsMenu.tsx, ColumnsMenu.tsx
      AvatarMenu.tsx, ResizeHandle.tsx, use-window-viewport.ts, view-components.ts
      shell-layout.ts, shell-actions.ts, sidebar-actions.ts, filter-groups.ts, phone-sheet-items.ts
      keyboard.ts               Global shortcuts
      dialogs/                  New project, new card, confirm, and the other dialogs
      notices/                  The notices panel and its rows
      palette/                  The command palette and its results
      toasts/                   The toast region

    layouts/                    (planned: AppRoot picks the layout by breakpoint today)
      use-breakpoint.ts         Current size: phone, tablet, or desktop
      DesktopLayout.tsx         Full sidebar, side panel, split view
      TabletLayout.tsx          Icon sidebar, overlay panels
      PhoneLayout.tsx           Top bar, bottom navigation, one pane

    data/                       The client data layer (architecture.md section 11.6)
      index.ts                  createData: builds the pieces below from what it is given
      api-client.ts             The one API client, one method for each route
      api-error.ts              ApiError, with a plain sentence for every failure
      event-stream.ts           The one WebSocket: hello, seq, resync, and reconnect
      stream-frames.ts          Reads frames from the daemon, builds the hello and the address
      stream-position.ts        The epoch and the last seq applied
      connection.ts             The connection state as Solid signals
      connection-machine.ts     The state machine behind it: checks, waits, and reconnecting
      page-signals.ts           The page coming back to the front, and the network returning
      token.ts                  The token in localStorage, and the dev token in memory
      dev-token-client.ts       Reads the dev token from the dev server (dev build only)
      daemon-clock.ts           The daemon's clock offset, for "4 min ago"
      optimistic.ts             Change the screen first, roll back on failure
      sections.ts               The per-section switch of the cutover register
      backoff.ts                Wait time between tries
      request-guard.ts          Time limit and cancel for one request
      timers.ts                 Timer functions that a test can replace
      guards.ts                 Small type checks for data from the wire
      storage.ts                Guarded reads and writes of localStorage
      mappers/
        time.ts                 Wire timestamps into milliseconds (and the relative time it borrows from the mock today)
        project.ts              Project into the shape the screens use
        agents.ts               Agent catalog into pickers and the mock's agent table
      testing/                  Fakes for tests
        fake-fetch.ts           A fetch that answers from a table of routes
        fake-web-socket.ts      A WebSocket that a test drives
        memory-storage.ts       localStorage in memory
        golden.ts               The golden files, as the app's tests read them
        connection-harness.ts   A connection machine on fake timers and a fake fetch
        stream-harness.ts       An event stream on a fake WebSocket
      queries/                  (planned: the sync layer calls the API client directly today)
        projects.ts, boards.ts, cards.ts, checklists.ts, comments.ts, members.ts, chats.ts,
        sessions.ts, home.ts, me.ts, integrations.ts, providers.ts, quality.ts, settings.ts

    sync/                       Mirrors the daemon into the store (architecture.md section 11.6)
      index.ts                  startSync: follows the connection, loads each section's snapshot, applies events, sets M.S.ready
      syncer.ts                 The shape of one section's sync (load, apply, event), and the list they join
      projects.ts               Section S3: apply the project list and project.* events, per-project screen state
      agents.ts                 Section S4: apply the agent catalog into S.agents, and the built-in agent the app appends to it
      project-actions.ts        Add, rename, save settings, and remove a project on the daemon, with optimistic updates
      connection-actions.ts     Sign in and Try again
      reservoir.ts              Keeps the mock cards, chats, notices, and feed items of projects the daemon does not have out of sight
      sync-control.ts           What the store keeps of its sync: reload and stop
      tests                     One test file per module above (agents, index, project-actions, projects)

    testing/                    Helpers for unit and component tests (never in a bundle)
      fake-daemon.ts            A daemon in memory: answers the real client and pushes events on the real stream
      projects.ts               Typed builders for projects, made from the golden project, and the prototype's three
      agents.ts                 Typed builders for the agent catalog, made from the golden agent, the prototype's three agents, and the golden catalog
      test-store.ts             A store made and filled the way the app's is, with or without a fake daemon
      install-test-store.ts     Makes window.M the shared test store in every unit test file

    stores/                     (planned: the theme, layout, and tour state live in the mock store today)
      theme-store.ts            Light, dark, system, custom
      layout-store.ts           Panels, split view, last view per project
      tour-store.ts             Tutorial progress

    views/
      home/
        HomeView.tsx            The dashboard across all projects
      chats/
        ChatsView.tsx           Chat list and open chat for a project
      agents/
        AgentsView.tsx          Table of all sessions
      board/
        BoardView.tsx           The project's board with filters and swimlanes
      list/
        ListView.tsx            Cards as a table
      timeline/
        TimelineView.tsx        Cards over time with dependencies
      calendar/
        CalendarView.tsx        Jobs, briefs, due cards, events
      card/
        CardRoute.tsx           Opens a card in the panel or full screen
      settings/
        SettingsView.tsx        Settings sections
        profile/
          ProfilePage.tsx       Name, avatar, email, time zone, devices, tailnet identity
        projects/
          ProjectsSettings.tsx  Project settings and remove
        roles/
          RolesSettings.tsx     Role templates
        providers/
          ProvidersSettings.tsx Provider keys and tests
        schedules/
          SchedulesSettings.tsx Schedules and briefs
        integrations/
          IntegrationsSettings.tsx Integrations and tests
        quality/
          QualitySettings.tsx   Smell profiles
        help/
          HelpSettings.tsx      Replay tour, shortcuts, docs links

    onboarding/
      Onboarding.tsx            The onboarding flow: the steps, the footer, and the draft
      onboarding-flow.ts        The steps and the order
      onboarding-draft.ts       The draft, its defaults (source: folder, no sample project), and validation
      onboarding-data.ts        The choices and fields the screens show
      WelcomeStep.tsx, ProfileStep.tsx, AgentsStep.tsx, ProjectStep.tsx, ControlStep.tsx
      StatusCheck.tsx, StepIntro.tsx, OnboardingFooter.tsx, StepProps.ts
      commit-step.ts            Turns the finished draft into a project on the daemon

    features/
      agents/
        agent-picker.ts         The rows of an agent picker (a missing agent disabled), the notes under it, and the model rows
        AgentNotes.tsx          The install command of a missing agent and the warning of an untested one, as small helper text
      chat/
        ChatThread.tsx, MessageRow.tsx, ToolCallRow.tsx, PlanBlock.tsx, ApprovalBlock.tsx
        CardLinks.tsx, CardPreview.tsx, DiffSummaryRow.tsx
      tutorial/
        Tour.tsx, TourCutout.tsx, TourPopover.tsx
        tour-controller.ts, tour-steps.ts, tour-target.ts, tour-geometry.ts
      projects/
        use-projects.ts         Create, rename, remove (planned: the sync layer holds this today)
      chats/
        use-chats.ts            Create, rename, archive, restore, delete (planned)
      board/
        use-board-drag.ts       Drag and drop wrapper (planned: the mock's drag layer holds this today)
      shortcuts/
        shortcuts.ts            Shortcut list shared by palette and settings (planned)

    lib/                        (planned: the mock's own helpers, such as format.ts, hold these today)
      format-cost.ts            Cost formatting
      format-time.ts            Relative and full times
      format-number.ts          Locale number formatting

    styles/
      app.css                   Imports tokens and Tailwind

  e2e/
    support/
      app.ts                    Sizes, themes, openApp, and the checks every screen must pass
      e2e-env.ts                The run's own daemon port, data folder, and process file
      daemon-api.ts             The real token, the project calls, and the agent catalog for a spec to start from
      git.ts                    Makes a temporary Git repository or plain folder, with no global Git settings
      global-setup.ts           Stops the run when the Vite server on the test port is not the one made for it
    daemon-connection.spec.ts   Loads from the daemon, can't reach it, sign in, and the offline bar: five sizes, both themes
    daemon-lifecycle.spec.ts    Stops and starts the real daemon and sees the app recover with no reload (runs alone, after the rest)
    daemon-projects.spec.ts     Add, refuse a repeat, rename, change settings, remove, the monorepo fixture, and two pages
    daemon-agents.spec.ts       The agent, model, and thinking pickers from the daemon's catalog, a missing and an untested agent, and the onboarding agents screen: five sizes, both themes
    responsive.spec.ts          Every view at three sizes, both themes
    onboarding.spec.ts          First launch, skip, and resume (planned)
    tour.spec.ts                Tutorial tour (planned)
    projects.spec.ts            Create, rename, remove (planned: `daemon-projects.spec.ts` covers this)
    chats.spec.ts               Create, rename, archive, restore, delete (planned)
    card-flow.spec.ts           Start card, plan, approve, review, merge (planned)
    checklists.spec.ts          Checklists, required to finish, agent ticks (planned)
    comments.spec.ts            Comments, attachments, links, agent replies (planned)
    members.spec.ts             Members and notices (planned)
    add-card.spec.ts            Add a card in each column and lane (planned)
    connections.spec.ts         Connection tests (planned: `daemon-connection.spec.ts` covers this)
```

---

## 5. Desktop shell

Not built yet: the Tauri shell is deferred until after Phase 2.

```
apps/desktop/
  package.json                  Tauri scripts
  src-tauri/
    Cargo.toml
    Cargo.lock
    rust-toolchain.toml         Pinned Rust version
    clippy.toml                 Clippy limits for smells
    build.rs
    tauri.conf.json             App settings, sidecar, updater, deep links
    capabilities/
      default.json              Allowed Tauri permissions
    icons/                      App icons
    binaries/                   The daemon sidecar, copied in at build time (ignored by Git)
    src/
      main.rs                   Window setup
      daemon.rs                 Checks and starts the daemon
      notifications.rs          OS notifications while the app is open
      deeplink.rs               marshal:// links
      updater.rs                App and daemon updates
```

---

## 6. Component library

**How to read this section.** The listing is the full planned component set. **Built today
(2026-09-25)** are: `base/` (Button, IconButton, Input, TextArea, Select, Checkbox, Switch,
SegmentedControl, Badge, Tag, Menu, MenuItem, MenuLabel, MenuSeparator, Skeleton, SkeletonLines,
SkeletonRow, SkeletonCard, SkeletonGroup, Toast, ToastRegion, EmptyState, ErrorState, NoResults,
NotConnected, NeedsBadge, CountBubble, Callout, ChoiceCard, Field, IconLabel, Kbd, Avatar,
PhoneList, PhoneRowTitle, PhoneRowMeta, ProgressBar, SortHeader, TableShell, TableRow, TableCell,
VirtualList, `virtual-window`,
`_Details`, `_RetryCountdown`, `cx`, `table-sort`, `types`), `layout/` (Dialog, Sheet, Scrim,
NavItem, DayHeading, ConnectionLost, OfflineBanner, SignIn, `focus-trap`, `_ScreenFrame`),
`board/` (CiStatus, StatusLabel), `card/` (DiffStat), `chat/` (Composer, JumpToLatest),
`dashboard/` (SummaryTile, BarChart, LineChart, ChartFigure, ChartLegend, FeedItem,
ShowMoreFooter, `chart-geometry`, `_ChartParts`), `icons/` (Icon, StatusDot, StatusIcon,
`icon-names`, `lucide-icons`, `status-glyphs`, `status`), and `settings/` (SettingsPanel,
SettingsSection, ItemText). Everything else in the listing is planned.

```
packages/ui/
  package.json
  tsconfig.json
  src/
    index.ts                    Public exports
    base/
      Button.tsx                    Actions
      IconButton.tsx                Icon-only actions
      Input.tsx                     Single-line text
      TextArea.tsx                  Multi-line text
      Select.tsx                    Pick one option
      Checkbox.tsx                  On and off choice in forms
      Switch.tsx                    Instant on and off setting
      SegmentedControl.tsx          Pick one of a few options inline
      Badge.tsx                     Small label or count
      Tag.tsx                       Removable filter or label chip
      Tooltip.tsx                   Short hint on hover or focus
      Menu.tsx                      Action list from a trigger
      Popover.tsx                   Floating panel
      Dialog.tsx                    Modal window
      ConfirmDialog.tsx             Confirm a risky action
      Tabs.tsx                      Switch between panels
      Skeleton.tsx                  Loading placeholder: one rounded pulsing block
      SkeletonLines.tsx             Loading placeholder for a paragraph, last line shorter
      SkeletonRow.tsx               Loading placeholder shaped like a list or project row
      SkeletonCard.tsx              Loading placeholder shaped like a board card
      SkeletonGroup.tsx             One "Loading" status around the shapes of one loading part
      Spinner.tsx                   Small loading marker
      Toast.tsx                     Result of a user action
      EmptyState.tsx                Nothing to show yet
      ErrorState.tsx                One section that failed to load: message, Try again, Details
      NotConnected.tsx              A service that is not set up, with a Connect button
      _Details.tsx                  Private: the expandable Details of ErrorState and ConnectionLost
      _RetryCountdown.tsx           Private: "Trying again in 4 s"
      Kbd.tsx                       Shows a keyboard shortcut
      Avatar.tsx                    Person or agent picture
      DataTable.tsx                 Sortable, virtualized table
      InlineRename.tsx              Rename in place
      UndoToast.tsx                 Toast with an undo action, used for archive
    layout/
      AppShell.tsx                  Sidebar, header, and content areas
      Sidebar.tsx                   Home, projects, notices, settings
      ProjectSwitcherItem.tsx       One project row with badges and a menu
      ViewHeader.tsx                Title, view switcher, filters, actions
      ViewSwitcher.tsx              Switch between views
      DetailPanel.tsx               Resizable right-side panel
      SplitView.tsx                 Up to four side-by-side panes
      ResizeHandle.tsx              Drag handle between panes
      TopBar.tsx                    Project name, search, notices, profile avatar
      MobileNav.tsx                 Bottom navigation on phones
      BottomSheet.tsx               Dialogs and menus on phones
      ResponsiveTable.tsx           Table on desktop, stacked items on phones
      BypassBanner.tsx              Striped warning for bypass mode
      ConnectionLost.tsx            Full screen when the app has no data and cannot reach the daemon
      OfflineBanner.tsx             Thin bar when the app has data and the connection dropped
      SignIn.tsx                    Full screen to paste the access token
      _ScreenFrame.tsx              Private: the centered full-screen shell of ConnectionLost and SignIn
    app/
      CommandPalette.tsx            Search actions, projects, cards, settings
      NoticeList.tsx                Notices in the sidebar
      NoticeGroup.tsx               Grouped notices with actions
      PairingCode.tsx               Code for pairing a phone
      ResourcePanel.tsx             RAM, CPU, and disk per card
    board/
      Board.tsx                     The kanban board
      BoardColumn.tsx               One column
      Swimlane.tsx                  A group row across columns
      CardItem.tsx                  A card on the board
      AddCardButton.tsx             Add a card, with a template button
      AddCardInline.tsx             Inline title field for a new card
      CardCounts.tsx                Checklist, comment, and attachment counts
      MemberAvatars.tsx             Member avatars with overflow
      StatusIcon.tsx                Flag icon for a card state
      StatusDot.tsx                 Small status dot
      CiBadge.tsx                   CI state on cards
      CostLabel.tsx                 Cost of a card or scope
      FilterBar.tsx                 Active filters and saved views
      SleepMarker.tsx               Moon marker for asleep cards
    card/
      CardDetail.tsx                The full card panel
      CardHeader.tsx                Title, state, quick actions
      SessionSettings.tsx           Agent, role, model, thinking, permissions selects
      PermissionModeSelect.tsx      Pick a permission mode
      ThinkingModeSelect.tsx        Pick low, medium, high, or extra high
      ChatTerminalToggle.tsx        Switch between chat and terminal
      ActivityFeed.tsx              Live list of agent actions
      ActivityItem.tsx              One action in the feed
      DoingNow.tsx                  The agent's current one-line status
      DiffView.tsx                  Rendered diff for many files
      DiffFile.tsx                  One file's diff
      ChecksList.tsx                Marshal's automatic acceptance checks
      SmellFindings.tsx             Code smell findings for a card, grouped by severity
      SmellFindingItem.tsx          One finding with location, reason, suggestion, and actions
      DismissReasonDialog.tsx       Dismiss a finding with a reason
      PreviewPane.tsx               Live preview in a built-in browser
      ScreenshotCompare.tsx         Before and after screenshots
      CheckpointList.tsx            Checkpoints with restore
      ContextMeter.tsx              How full the context window is
      NotesPanel.tsx                Card notes from the vault
    chat/
      ChatThread.tsx                Scrollable, virtualized message list
      ChatMessage.tsx               One user or agent message
      ChatComposer.tsx              Message input and send
      ToolCallBlock.tsx             Collapsed tool call row
      PlanBlock.tsx                 Plan with approve, edit, reject
      ApprovalBar.tsx               Inline approval request
      CardPreview.tsx               Small card shown inside chat
      JumpToLatest.tsx              Button to return to newest output
      MarkdownContent.tsx           Sanitized markdown
      CodeBlock.tsx                 Highlighted code
      AgentPicker.tsx               Choose who a chat talks to
      ChatList.tsx                  List of chats with search and archived section
      ChatListItem.tsx              One chat row with menu
      NewChatButton.tsx             Start a chat and pick who to talk to
    terminal/
      TerminalView.tsx              Lazy-loaded xterm.js terminal
    dashboard/
      NeedsYouList.tsx              Items waiting on the user, grouped by project
      SummaryTile.tsx               One number with a short label, opens a filtered view
      RangeControl.tsx              Switch between 7, 30, and 90 days
      BarChart.tsx                  Simple bar chart in SVG
      LineChart.tsx                 Simple line chart in SVG, with an optional limit line
      Sparkline.tsx                 Tiny trend line inside tiles and rows
      ActivityStream.tsx            Live, virtualized feed across projects
      ActivityStreamItem.tsx        One activity with its link
      ComingUpList.tsx              Today's events, jobs, briefs, and due cards
      AwakeAgentsList.tsx           Awake agents with sleep actions and the limit
      CiHealthList.tsx              Main branch state per project
    projects/
      ProjectDialog.tsx             Create a project
      RemoveProjectDialog.tsx       Confirm removing a project
      ProjectSettings.tsx           Edit a project
    timeline/
      DependencyLine.tsx            Line between dependent cards
    settings/
      SettingsLayout.tsx            Settings sections and navigation
      RoleEditor.tsx                Edit a role template
      TemplateEditor.tsx            Edit a card template
      ProviderKeyField.tsx          Add, change, and test an API key
      LimitField.tsx                Set a cost or awake limit
      SmellProfileEditor.tsx        Checks, thresholds, and severity per project and language
      ScheduleEditor.tsx            Create or edit a schedule
      IntegrationCard.tsx           Connect, manage, and test an integration
      TestConnectionButton.tsx      Runs a connection test, with cooldown
      ConnectionTestResult.tsx      List of checks with results and fix hints
      McpServerRow.tsx              One MCP server with health
      SkillRow.tsx                  One installed skill
      ThemePicker.tsx               Light, dark, system, custom
    onboarding/
      OnboardingStep.tsx            One onboarding screen
      StepIndicator.tsx             Shows "Step 2 of 4"
      AgentDetectList.tsx           Detected CLI agents with versions
      PairDevicePanel.tsx           Pair a phone during onboarding or in settings
    tutorial/
      TutorialTour.tsx              Runs the Home dashboard tour
      Spotlight.tsx                 Dims the page and highlights one control
      TourPopover.tsx               Step title, text, count, Back, Next, Skip tour
    profile/
      ProfileAvatarButton.tsx       Avatar in the top right, opens the profile
      DeviceList.tsx                Paired devices with remove
    collab/
      ChecklistsTab.tsx             All checklists, then acceptance checks and smells
      Checklist.tsx                 One named checklist
      ChecklistItem.tsx             One item with who ticked it
      AddChecklistItem.tsx          Inline field to add items
      AddChecklistMenu.tsx          Add a checklist by type
      ChecklistProgress.tsx         Progress bar with count
      EvidenceLink.tsx              Evidence behind an agent tick
      CommentsTab.tsx               Comment list and composer
      CommentItem.tsx               One comment
      CommentComposer.tsx           Text, attach, link, mentions
      MentionSuggest.tsx            Mention suggestions
      AttachmentPreview.tsx         Image preview or file row
      LinkChip.tsx                  Link as site name and text
      AgentReadMarker.tsx           Agent read this
      MembersRow.tsx                People and the agent in the card header
      MemberPicker.tsx              Add or remove people
    icons/
      Icon.tsx                Lucide wrapper with token sizes
      status/
        DashedCircle.tsx      Backlog
        FlagOutline.tsx       Planning
        FlagFilled.tsx        Working
        RaisedHand.tsx        Needs you
        Eye.tsx               In review
        MergeArrows.tsx       Ready to merge and merging
        CheckeredFlag.tsx     Done
        Moon.tsx              Asleep
```

Screen-level components (`HomeView`, `ChatsView`, `AgentsView`, `ListView`, `TimelineView`, `CalendarView`, `ProfilePage`, and `OnboardingFlow`) live in `apps/web` (section 4), because they are screens, not reusable parts.

---

## 7. Tokens

```
packages/tokens/
  package.json
  tsconfig.json
  vitest.config.ts
  src/
    tokens.ts                   Source of truth for every value in ui-tokens.md
    themes.ts                   Light and dark theme maps
    contrast.ts                 The contrast rules as code
    contrast-exceptions.ts      The design-approved pairs that miss the targets, and why (CI fails on a new miss or a stale entry)
    fonts.css                   The typeface faces and their fallbacks
  scripts/
    build-css.ts                Generates CSS variables and the Tailwind theme
    check-contrast.ts           Contrast rules check, used in CI and for custom themes
  test/
    tokens.test.ts              Every token value
    contrast.test.ts            The rules and the exceptions list
  dist/
    tokens.css                  Generated. Do not edit.
    tailwind-theme.css          Generated. Do not edit.
```

The typefaces come from the `@fontsource/atkinson-hyperlegible-next` and `@fontsource/atkinson-hyperlegible-mono` packages, imported by `src/fonts.css`, so nothing loads from a CDN and the app works offline.

---

## 8. Protocol

```
packages/protocol/
  package.json
  tsconfig.json
  vitest.config.ts
  src/
    index.ts                    Exports
    generated/                  Generated from Go by tygo (pnpm gen). Do not edit.
  test/
    golden.ts                   Reads the daemon's golden files
    golden.test.ts              Checks each golden file against the generated types
    enums.test.ts               Checks the generated <Type>Values arrays against the Go lists, and the timestamp format
    agents.test.ts              The agent catalog wire types
    card-actions.test.ts        The message body and its length limit
    session.test.ts             The session event payloads
```

---

## 9. Tools

Each program is its own Go module, with no `go.work`, and each has a `package.json` with scripts only (`build`, `test`, `lint:go`, `format:go`, `format:check:go`).

```
tools/
  stub-agent/
    go.mod
    main.go                     Flags and start-up
    agent.go                    The ACP agent: initialize, sessions, prompt, cancel
    unsupported.go              The ACP methods it does not support
    scenario.go                 Loading scenarios and choosing one
    runner.go                   Running a scenario's steps
    session.go                  Session state saved to disk, so a restart can resume
    output.go                   Writes lines to the terminal
    scenarios/                  Embedded in the program (JSON, not YAML, to add no dependency)
      default.json              Reads, summarizes, and edits a file
      approve.json              Asks for approval
      fail-test.json            Fails a test, then fixes it
      stuck-loop.json           Repeats the same error
      resume.json               Says its turn number, so tests can prove context survived a restart
      smells.json               Writes code with known smells
  budgets/
    go.mod
    main.go                     Flags and the budget report
    measure.go                  Starts the daemon, lets it idle, and reads its memory and processor use
    parse.go                    Reads ps, /proc, and PowerShell output
    sample.go                   The shared sampling loop and its report type
    sample_darwin.go, sample_linux.go, sample_windows.go, sample_other.go
  hooks-replay/
    go.mod
    main.go                     Flags and start-up
    replay.go                   Reads a recorded webhook and sends it to the dev daemon
```

---

## 10. Scripts

```
scripts/
  setup-tools.mjs               Installs the pinned Go tools into .tools/
  go-tool.mjs                   Runs a Go tool from .tools/bin, the same way on every platform
  gen-protocol.mjs              Generates packages/protocol from the Go types with tygo, adds the <Type>Values arrays, then formats it
  smells.mjs                    Runs all smell tools (Biome, golangci-lint, knip, jscpd) and the file length check
  budgets.mjs                   Web build size, then the daemon's idle memory and processor use
  check.mjs                     Runs everything CI runs, in order (eleven steps, the light ones first)
  check-generated.mjs           Checks that a second pnpm gen changes nothing, or (in CI) that the generated files match what Git holds
  coverage.mjs                  Checks the daemon's test coverage per package against the floors in docs/code-standards.md
  agent-smoke.mjs               Runs the real agent CLIs (or, with --stub, the stub agent) through one small card each on a throwaway daemon
  e2e-daemon.mjs                Runs the built dev daemon for the end-to-end tests, on port 47811 with a data folder of its own
```

There is no `gen.mjs`, `dev-reset.mjs`, or `build.mjs`: `pnpm gen` and `pnpm build` are chains of package scripts, and `marshal dev reset` does the dev reset in Go, so the folder rules live in one place.
