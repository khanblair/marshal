// Package buildinfo says which version of Marshal is running.
package buildinfo

// Version is the one version number that the daemon, the command line tool, and the app share.
const Version = "0.0.0"
