{
  "description": "Writes a TypeScript file with obvious code smells: a function of 120 lines and magic numbers.",
  "steps": [
    {
      "type": "say",
      "text": "I will write the report builder."
    },
    {
      "type": "pause",
      "ms": 400
    },
    {
      "type": "tool",
      "id": "write-report",
      "title": "Write src/report.ts",
      "kind": "edit",
      "write": {
        "path": "src/report.ts",
        "lines": [
          "// Code written by the stub agent with known smells: a very long function and magic numbers.",
          "export interface Order {",
          "  amount: number;",
          "  region: string;",
          "}",
          "",
          "export function buildReport(orders: Order[], days: number): string {",
          "  const seconds = days * 86400;",
          "  let total = 0;",
          "  let text = `Report for ${seconds} seconds\\n`;",
          "  if (orders.length > 0) {",
          "    total += orders[0].amount * 1.0725;",
          "    text += `order 0: ${orders[0].amount} in ${orders[0].region}\\n`;",
          "  }",
          "  if (orders.length > 1) {",
          "    total += orders[1].amount * 1.0725;",
          "    text += `order 1: ${orders[1].amount} in ${orders[1].region}\\n`;",
          "  }",
          "  if (orders.length > 2) {",
          "    total += orders[2].amount * 1.0725;",
          "    text += `order 2: ${orders[2].amount} in ${orders[2].region}\\n`;",
          "  }",
          "  if (orders.length > 3) {",
          "    total += orders[3].amount * 1.0725;",
          "    text += `order 3: ${orders[3].amount} in ${orders[3].region}\\n`;",
          "  }",
          "  if (orders.length > 4) {",
          "    total += orders[4].amount * 1.0725;",
          "    text += `order 4: ${orders[4].amount} in ${orders[4].region}\\n`;",
          "  }",
          "  if (orders.length > 5) {",
          "    total += orders[5].amount * 1.0725;",
          "    text += `order 5: ${orders[5].amount} in ${orders[5].region}\\n`;",
          "  }",
          "  if (orders.length > 6) {",
          "    total += orders[6].amount * 1.0725;",
          "    text += `order 6: ${orders[6].amount} in ${orders[6].region}\\n`;",
          "  }",
          "  if (orders.length > 7) {",
          "    total += orders[7].amount * 1.0725;",
          "    text += `order 7: ${orders[7].amount} in ${orders[7].region}\\n`;",
          "  }",
          "  if (orders.length > 8) {",
          "    total += orders[8].amount * 1.0725;",
          "    text += `order 8: ${orders[8].amount} in ${orders[8].region}\\n`;",
          "  }",
          "  if (orders.length > 9) {",
          "    total += orders[9].amount * 1.0725;",
          "    text += `order 9: ${orders[9].amount} in ${orders[9].region}\\n`;",
          "  }",
          "  if (orders.length > 10) {",
          "    total += orders[10].amount * 1.0725;",
          "    text += `order 10: ${orders[10].amount} in ${orders[10].region}\\n`;",
          "  }",
          "  if (orders.length > 11) {",
          "    total += orders[11].amount * 1.0725;",
          "    text += `order 11: ${orders[11].amount} in ${orders[11].region}\\n`;",
          "  }",
          "  if (orders.length > 12) {",
          "    total += orders[12].amount * 1.0725;",
          "    text += `order 12: ${orders[12].amount} in ${orders[12].region}\\n`;",
          "  }",
          "  if (orders.length > 13) {",
          "    total += orders[13].amount * 1.0725;",
          "    text += `order 13: ${orders[13].amount} in ${orders[13].region}\\n`;",
          "  }",
          "  if (orders.length > 14) {",
          "    total += orders[14].amount * 1.0725;",
          "    text += `order 14: ${orders[14].amount} in ${orders[14].region}\\n`;",
          "  }",
          "  if (orders.length > 15) {",
          "    total += orders[15].amount * 1.0725;",
          "    text += `order 15: ${orders[15].amount} in ${orders[15].region}\\n`;",
          "  }",
          "  if (orders.length > 16) {",
          "    total += orders[16].amount * 1.0725;",
          "    text += `order 16: ${orders[16].amount} in ${orders[16].region}\\n`;",
          "  }",
          "  if (orders.length > 17) {",
          "    total += orders[17].amount * 1.0725;",
          "    text += `order 17: ${orders[17].amount} in ${orders[17].region}\\n`;",
          "  }",
          "  if (orders.length > 18) {",
          "    total += orders[18].amount * 1.0725;",
          "    text += `order 18: ${orders[18].amount} in ${orders[18].region}\\n`;",
          "  }",
          "  if (orders.length > 19) {",
          "    total += orders[19].amount * 1.0725;",
          "    text += `order 19: ${orders[19].amount} in ${orders[19].region}\\n`;",
          "  }",
          "  if (orders.length > 20) {",
          "    total += orders[20].amount * 1.0725;",
          "    text += `order 20: ${orders[20].amount} in ${orders[20].region}\\n`;",
          "  }",
          "  if (orders.length > 21) {",
          "    total += orders[21].amount * 1.0725;",
          "    text += `order 21: ${orders[21].amount} in ${orders[21].region}\\n`;",
          "  }",
          "  if (orders.length > 22) {",
          "    total += orders[22].amount * 1.0725;",
          "    text += `order 22: ${orders[22].amount} in ${orders[22].region}\\n`;",
          "  }",
          "  if (orders.length > 23) {",
          "    total += orders[23].amount * 1.0725;",
          "    text += `order 23: ${orders[23].amount} in ${orders[23].region}\\n`;",
          "  }",
          "  if (orders.length > 24) {",
          "    total += orders[24].amount * 1.0725;",
          "    text += `order 24: ${orders[24].amount} in ${orders[24].region}\\n`;",
          "  }",
          "  if (orders.length > 25) {",
          "    total += orders[25].amount * 1.0725;",
          "    text += `order 25: ${orders[25].amount} in ${orders[25].region}\\n`;",
          "  }",
          "  if (orders.length > 26) {",
          "    total += orders[26].amount * 1.0725;",
          "    text += `order 26: ${orders[26].amount} in ${orders[26].region}\\n`;",
          "  }",
          "  if (orders.length > 27) {",
          "    total += orders[27].amount * 1.0725;",
          "    text += `order 27: ${orders[27].amount} in ${orders[27].region}\\n`;",
          "  }",
          "",
          "  // The two constants above are typed in on purpose: the file is a test fixture.",
          "  return `${text}total: ${total}`;",
          "}"
        ]
      },
      "result": "Wrote src/report.ts"
    },
    {
      "type": "say",
      "text": "Done. The report builder is in src/report.ts."
    }
  ]
}
